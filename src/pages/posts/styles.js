export default (theme) => ({
  menu: {
    maxHeight: 300
  },
  toolbar: {
    display: 'flex',
    justifyContent: 'flex-end',
    marginBottom: theme.spacing(1)
  },
  dialog: {
    padding: theme.spacing(4)
  }
})
